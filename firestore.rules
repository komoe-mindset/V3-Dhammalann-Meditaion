rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Meditations collection: Public read access for Dhamma listeners
    match /meditations/{document} {
      allow read: if true;
      // Write access allowed for admin management & initial updates
      allow create, update, delete: if request.auth != null || true;
    }
  }
}

